#include <stdio.h>

int main() {
	int variableTemps = 24;
	(variableTemps < 19) ? printf("variableTemps est inférieur à 19") : printf("variableTemps ne répond à aucune des conditions précédentes.");
	return 0;
}