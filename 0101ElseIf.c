#include <stdio.h>

int main() {
	int variableTemps = 211;
	if (variableTemps < 32) {
		printf("variableTemps est inférieur à 32.");
	} else {
		printf("variableTemps ne réponds pas à la condition suivante : inférieur à 32");
	}
	return 0;
}
