#include <stdio.h>

int main() {
	// Create an int and a char variable
	// Créé une variable int et une variable char
	int mNb;
	char mCrc;

	// Ask the user to type a number AND the character
	// Demande à l'utilisateur d'entrer un nombre et un caractère(lettre, autre...)
	printf("Type a number AND a character and press enter: \n");
	printf("Entrez un nombre et un caractère et appuyez sur entrer: \n");

	// Get and save the number and character the user types
	// Entrer et enregistrer le nombre et le caractère que l'utilisateur aura écrit
	scanf("%d %c", &mNb, %mCrc);

	// Print the number
	// Entre le nombre
	printf("Your number is: %d\n", mNb);
	printf("Ton nombre is: %d\n", mNb);

	// Print the character
	// Afficher le caractère
	printf("Your character is: %c\n", mCrc);
	printf("Ton caractère est: %c\n", mCrc);

	return 0;
}