#include <stdio.h>

int main() {
	int monNumero = 6;

	if (monNumero % 2 == 0) {
		printf("%d est pair. \n", monNumero);
	} else {
		printf("%d est impair. \n", monNumero);
	}

	return 0;
}